<section class="grid grid-cols-5 gap-10">
    {{#each filenamesList}}
        <button class="btn btn-circle w-[64px] h-[64px]">
            <img src="/images/{{this}}" alt="{{this}}" width="48" height="48">
        </button>
    {{/each}}
</section>
